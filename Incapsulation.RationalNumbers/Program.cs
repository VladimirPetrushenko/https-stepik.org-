using System.Collections.Generic;
using System.Linq;
using NUnitLite;
using Incapsulation.RationalNumbers;
class Program
{
	static void Main(string[] args)
	{
        new AutoRun().Execute(args);
        //int m = (new Rational(-1, 4) / new Rational(3, 2)).Numerator;
        //System.Console.WriteLine((new Rational(-1, 4) / new Rational(3, 2)).Numerator + "  " + (new Rational(-1, 4) / new Rational(3, 2)).Denominator);
        //System.Console.ReadLine();
	}
}
